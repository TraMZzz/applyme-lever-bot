# === Internal — NOT part of the deliverable (kept local only) ===
# ApplyMe's internal take-home brief
test.pdf
# internal research dossier + captured raw company HTML
ref/
# internal agent/working guide (not for the grader)
CLAUDE.md
# internal brainstorm/spec/plan working artifacts (ARCHITECTURE.md + REPORT.md ship; these don't)
docs/superpowers/

# === Provided inputs (candidate PII + specific target links) ===
profile.md
resume.md
vacancies.md
data/

# === Local tooling / agent config (not part of the deliverable) ===
.claude/

# === Secrets ===
.env
*.key
*.pem

# === Run artifacts / evidence (regenerated per run) ===
output/
*.log
*.har

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.egg-info/

# === OS / editor ===
.DS_Store
.idea/
.vscode/
