# === Internal — NOT part of the deliverable (kept local only) ===
# internal research dossier + captured raw company HTML
ref/
# internal agent/working guide (not for the grader)
CLAUDE.md
# internal brainstorm/spec/plan working artifacts (ARCHITECTURE.md + REPORT.md ship; these don't)
docs/superpowers/

# === Inputs (candidate PII + target links) — raw provided material + generated bot inputs ===
# raw provided files: profile.md, resume.md, vacancies.md, test.pdf
inputs/
# generated by scripts/prepare_inputs.py: profile.json, resume.pdf, vacancies.txt
data/

# === Local tooling / agent config (not part of the deliverable) ===
.claude/

# === Secrets ===
.env
*.key
*.pem

# === Run artifacts / evidence (regenerated per run) ===
output/
*.log
*.har

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.egg-info/

# === OS / editor ===
.DS_Store
.idea/
.vscode/
