# Run screenshots — the 5 postings + the sandbox submit

Evidence for the brief's "screenshots of the 5 applications with results". Generated by the bot
(`output/<company>/<id>/final.png`) and copied here. Candidate data is **synthetic** (a fabricated
test profile — "Ethan Calder"); the `/thanks` and `already-received` pages don't echo applicant data.

These are **`--submit-mode` runs against the 5 real postings** (the brief directs applying to them) plus
the `leverdemo` sandbox:

| # | File | Posting | Result |
|---|---|---|---|
| 1 | `1-aledade-duplicate.png` | aledade — Staff AI Researcher | **`DUPLICATE_SUSPECTED`** — already applied (Lever → `/already-received`) |
| 2 | `2-raptv-duplicate.png` | raptv — Social Editor (Pop Hive) | **`DUPLICATE_SUSPECTED`** — already applied |
| 3 | `3-padsplit-no-submit.png` | padsplit — Senior Copywriter | **`FAILED` (fail-closed)** — a 19-question required screening survey the profile doesn't fit; the bot declines to fabricate answers, so **no POST** is sent |
| 4 | `4-skillerszone-success.png` | skillerszone — Remote Data Entry | **`SUCCESS`** — "Application submitted!" (`/thanks`) |
| 5 | `5-theathletic-duplicate.png` | theathletic — Executive Producer | **`DUPLICATE_SUSPECTED`** — already applied |
| 6 | `6-leverdemo-submit-success.png` | leverdemo sandbox — submit test | **`SUCCESS`** — invisible Enterprise hCaptcha **silent-passed**, application accepted |

What the set demonstrates: the bot genuinely **submits end-to-end on a real posting** (skillerszone → "Application
submitted!"), correctly recognizes **duplicates** (aledade/theathletic/raptv were submitted on earlier runs), and
**fail-closes with integrity** (padsplit — it won't fabricate screening answers for an ill-fitting profile, so it
sends no application and reports exactly which required fields it couldn't answer). `leverdemo` (#6) is the clean
captcha proof: the invisible hCaptcha silent-passes and the submit is accepted. The per-vacancy `result_string`
for every run is in `output/results.json`. Full reasoning + the submit-button root-cause: [`docs/REPORT.md`](../docs/REPORT.md) §0 + §4.
